@{
	ViewBag.Title = "/Admin/Index";
}

@section HeaderImage
{
	<div id="logo">
		<h1>Admin</h1>
	</div>
}

	<h3>/Admin/Index</h3>
	<br />

	<p><a href='@Url.Action("Logs", "Admin")'>Admin Log Manager</a></p>

	<p><a href='#' id="warm">Warm up Cache</a></p>

	<ul id="messages"></ul>

	<script type="text/javascript">
		$(function () {
			var connection = $.connection('echo');

			connection.received(function (data) {
				$('#messages').append('<li>' + data + '</li>');
				Utility.notify(data, 'Client Connection', 'bottom-right');
			});

			connection.start();

			$('#broadcast').click(function () {
				connection.send($('#msg').val());
			});
		});
	</script>
@section ScriptSection
{
    <script type="text/javascript">
    	$('a#warm').click(function (e) {
    		e.preventDefault();

    		// Load sammichele
    		$.get('@Url.Action("List", "Blog", new { user = "sammichele"} )', function (data) {
    			Utility.notify('sammichele blog list warmed', 'Warm Cache', 'bottom-right');
    		});

    		// Load sammichele
    		$.get('@Url.Action("List", "Blog", new { user = "bekah"} )', function (data) {
    			Utility.notify('bekah blog list warmed', 'Warm Cache', 'bottom-right');
    		});
    	});
	</script>
}